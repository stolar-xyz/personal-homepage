import React from 'react'
import { IconButton, Wrapper } from './styled';

const IconButtons = () => (
    <Wrapper>
        <IconButton>
            click me!
    </IconButton>
        <IconButton>
            click me!
    </IconButton>
        <IconButton>
            click me!
    </IconButton>
        <IconButton>
            click me!
    </IconButton>
    </Wrapper>
);

export default IconButtons;