import React from 'react'
import { IconButton } from './styled';

const IconButtons = () => (
    <div>
        <IconButton>
            click me!
        </IconButton>
        <IconButton>
            click me!
        </IconButton>
        <IconButton>
            click me!
        </IconButton>
        <IconButton>
            click me!
        </IconButton>
    </div>
);

export default IconButtons;